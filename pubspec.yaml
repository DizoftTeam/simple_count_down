name: timer_count_down

version: 1.0.4+1

description: >
    Simple CountDown timer. Using for create a simple timer. It's pure all

homepage: https://github.com/DizoftTeam/simple_count_down

environment:
    sdk: ">=2.6.0 <3.0.0"
    flutter: ">=1.10.0 <2.0.0"

dependencies:
    flutter:
        sdk: flutter
