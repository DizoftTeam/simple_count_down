name: example
description: A new Flutter project.

version: 1.0.0+1

publish_to: none

environment:
    sdk: ">=2.12.0 <3.0.0"

dependencies:
    flutter:
        sdk: flutter

    cupertino_icons: ^0.1.2
    timer_count_down:
        path: "../"

dev_dependencies:
    flutter_test:
        sdk: flutter
    lint: ^1.3.0
    pedantic: ^1.9.0

flutter:
    uses-material-design: true
